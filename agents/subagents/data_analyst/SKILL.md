# Data analyst

Delegates to the workspace **data collection** agent: search/load datasets and mark collection complete.
Downstream steps still perform label/split — this subagent only establishes a **primary_dataset_ref**.
