# Monitor

Compare **reference** vs **current** registered datasets with `evidently_data_drift`.
Use `evidently_model_performance` when you have prediction columns on both frames.
Open a `open_retrain_request` only when drift or performance clearly breaches tolerances described in the user context.
